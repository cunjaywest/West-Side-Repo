// ── Cloud sync setup ──
// After creating your Supabase project, go to: Settings → API
// Copy "Project URL" and the "anon public" key, paste them below,
// then re-upload this one file to GitHub. Every app on your hub
// reads from here, so you only ever edit this in one place.

window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
